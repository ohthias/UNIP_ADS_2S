#include <stdio.h>
int main(){
	float raio, area;
	printf("Digite o raio em centímetros: ");
	scanf("%2f",&raio);
	
	area = (3.14*raio*raio);
	printf("O valor da área em centímetros é de %2f", area);
	}
